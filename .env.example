DATABASE_URL=sqlite:///./market_opportunity.db
CORS_ORIGINS=http://localhost:5173
# Leave empty on Vercel so the frontend calls same-origin /api routes.
VITE_API_BASE=http://localhost:8000
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=alerts@market-opportunity.local

# Vercel automatically sets VERCEL=1 and VERCEL_URL during deployments.
